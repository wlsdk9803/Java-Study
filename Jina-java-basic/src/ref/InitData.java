package ref;

public class InitData {
    // 멤버변수는 인스턴스를 생성할 때 자동으로 초기화 된다. (int형은 0으로, boolean형은 false로, 참조형은 null로)
    // 지역변수는 꼭 초기화 해줘야 함..!
    int value1; // 초기화 하지 않음
    int value2 = 10; // 10으로 초기화
}
