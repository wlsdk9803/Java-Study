package static2;

public class DecoUtil2 {
    public static String deco(String str){
        return "*" + str + "*";
    } // 클래스 메서드
    // static이 붙어서 정적 메서드가 됨. 정적 변수처럼 인스턴스 생성 없이 클래스 명을 통해서 바로 호출 가능
}
